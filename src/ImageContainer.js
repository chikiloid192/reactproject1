
import Button from 'react-bootstrap/Button';
export function ImageContainer(props) {
  return (
    <div>
      {' '}
      <Button variant="outline-warning">Начать игру</Button> ImageContainer
    </div>
  );
}
